@model IEnumerable<BlogMVC.Models.Post>
@{
    ViewData["Title"] = "Mon blog";
}

<div class="page-header">
    <h1>Blog</h1>
    <p class="lead">Welcome on my blog</p>
</div>

@foreach (var item in Model)
{
    @Html.DisplayFor(modelItem => item, "PostPreview")
}
